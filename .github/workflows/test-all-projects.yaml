name: Run Tests For All Projects 3
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read
  pull-requests: write
  checks: write
  
jobs:
  run-tests:
    uses: rplazaman/poweretl-pipelines/.github/workflows/tests.yaml@test
    with:
      python-version: '3.13'
      projects: '["utils","common"]'