name: Use Shared Test Workflow

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  run-tests:
    uses: rplazaman/poweretl-pipelines/.github/workflows/run-tests.yaml@test
    with:
      python-version: '3.13'
      projects: '["utils","common"]'